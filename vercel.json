{
  "directory": "frontend",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "${REACT_APP_BACKEND_URL}/api/$1"
    }
  ]
}